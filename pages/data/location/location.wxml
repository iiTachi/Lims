<view class='location'>
  <map
      id="myMap"
      style="width: 100%; height: 600px;"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      show-location
    ></map>
</view>